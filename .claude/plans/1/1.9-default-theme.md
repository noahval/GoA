# Default Theme

**Goal**: Create unified UI theme resource (default_theme.tres) with orange/blue/red color scheme, clear button states, and consistent styling across all scenes

**Success Criteria**:
- default_theme.tres created with complete style definitions
- Orange/blue/red color scheme implemented
- Five button types defined: Standard, Affirmative, ForwardNav, BackNav, Danger
- All button states defined: Normal, Hover, Pressed, Disabled
- Font sizes reference ResponsiveLayout (Phase 1.10)
- Panel opacity: 30% for translucent overlay aesthetic
- Theme applied globally via project settings

**Prerequisites**:
- Phase 1.1 (Project Setup) - Project and resolution configured
- Phase 1.10 (Responsive Layout) - Font sizing and button dimensions defined

---

## Overview

Establishes the visual foundation for GoA's UI. The default theme provides consistent colors, typography, and interaction states across all scenes. Uses an orange/blue color palette that reinforces action hierarchy: orange for standard/negative actions, blue for affirmative/navigation actions.

### Key Design Principles

- **Orange/blue/red color scheme**: Orange base, blue accents for affirmative/nav, red for danger
- **Visual hierarchy through color**: Orange = standard, Blue = affirmative/navigation, Red = danger/destructive
- **Border thickness communicates importance**: 2px forward nav > 1px back nav
- **Translucent overlays**: 30% opacity maintains layered aesthetic
- **Unified font size**: Defined by ResponsiveLayout (Phase 1.10), 25px base, scales automatically
- **Obviously disabled**: Grey buttons with low opacity, clearly unclickable
- **Orange theme variations**: Stick with orange for now, can create regional theme variations later as needed

---

## Color Palette

### Base Colors

```gdscript
# Orange (Standard buttons, panels, labels)
dark_orange = Color(0.361, 0.180, 0, 0.2)        # Normal state
light_orange = Color(0.42, 0.25, 0.05, 0.2)      # Hover state
orange_border = Color(0.6, 0.38, 0.15, 0.45)     # Border color

# Blue (Affirmative buttons - confirm, accept, yes)
affirmative_blue = Color(0.008, 0.416, 0.62, 0.2)     # Normal: #026a9e
affirmative_hover = Color(0.1, 0.52, 0.75, 0.2)       # Hover
affirmative_pressed = Color(0.006, 0.33, 0.5, 0.35)   # Pressed

# Light Blue (Forward navigation - enter, explore, advance)
forward_nav_blue = Color(0.255, 0.412, 0.882, 0.2)    # Normal: #4169E1 (royal blue)
forward_nav_border = Color(0.357, 0.608, 0.835, 1.0)  # Border: #5B9BD5 (bright blue), 2px
forward_nav_text = Color(0.529, 0.808, 0.922, 1.0)    # Text: #87CEEB (sky blue)

# Navy Blue (Back navigation - return, exit, leave)
back_nav_navy = Color(0, 0, 0.502, 0.3)               # Normal: #000080 (navy)
back_nav_border = Color(0.176, 0.290, 0.431, 1.0)     # Border: #2D4A6E (blue-gray), 1px
back_nav_text = Color(0.690, 0.769, 0.871, 1.0)       # Text: #B0C4DE (steel blue)

# Red (Danger buttons - delete, destroy, irreversible actions)
danger_red = Color(0.8, 0.1, 0.1, 0.25)               # Normal: #CC1A1A (dark red)
danger_hover = Color(0.9, 0.2, 0.2, 0.3)              # Hover: brighter red
danger_pressed = Color(0.6, 0.05, 0.05, 0.35)         # Pressed: darker red
danger_border = Color(1.0, 0.3, 0.3, 0.6)             # Border: bright red warning

# Grey (Disabled buttons - obviously unclickable)
disabled_grey = Color(0.3, 0.3, 0.3, 0.15)       # Low opacity background
disabled_text = Color(0.5, 0.5, 0.5, 0.6)        # Dimmed text

# Text Colors
text_normal = Color(1, 1, 1, 1)                  # White
text_hover = Color(1, 1, 0.8, 1)                 # Light yellow
text_pressed = Color(0.9, 0.9, 0.9, 1)           # Light grey
```

### Design Philosophy

**Why Orange/Blue?**
- **Orange** = Warm, attention-grabbing, standard/neutral actions
- **Blue** = Cool, trustworthy, affirmative/navigation actions
- Creates natural visual hierarchy without thinking

**Why Different Blues?**
- **Light blue (forward nav)** = Progress, advancement, moving deeper
- **Navy blue (back nav)** = Retreat, return, going back
- Directional color temperature (light = forward, dark = back)

**Why Border Thickness Matters?**
- **2px border (forward nav)** = Emphasizes primary action
- **1px border (back nav)** = De-emphasizes secondary action
- Thickness creates hierarchy at a glance

---

## Button Types

### 1. Standard Button (Orange)

**Usage**: Default for cancel, decline, neutral, or negative actions

**States**:
```gdscript
# Normal
background: Color(0.361, 0.180, 0, 0.2)       # Dark orange, 20% opacity
border: 1px, Color(0.6, 0.38, 0.15, 0.45)
corner_radius: 3px
text_color: Color(1, 1, 1, 1)                 # White

# Hover
background: Color(0.42, 0.25, 0.05, 0.2)      # Lighter orange, 20% opacity
border: 1px, Color(0.7, 0.45, 0.2, 0.5)       # Brighter border
text_color: Color(1, 1, 0.8, 1)               # Light yellow

# Pressed
background: Color(0.30, 0.15, 0, 0.25)        # Darker orange, 25% opacity
border: 1px, Color(0.5, 0.3, 0.1, 0.5)
text_color: Color(0.9, 0.9, 0.9, 1)           # Light grey

# Disabled
background: Color(0.3, 0.3, 0.3, 0.15)        # Grey, 15% opacity
border: None or 1px subtle grey
text_color: Color(0.5, 0.5, 0.5, 0.6)         # Dimmed grey
```

**Examples**: Cancel, Back, Close, Delete, No

### 2. Affirmative Button (Blue)

**Usage**: Positive actions in dialogs and confirmations

**States**:
```gdscript
# Normal
background: Color(0.008, 0.416, 0.62, 0.2)    # Blue #026a9e, 20% opacity
border: 1px (optional)
corner_radius: 3px
text_color: Color(1, 1, 1, 1)                 # White

# Hover
background: Color(0.1, 0.52, 0.75, 0.2)       # Lighter blue, 20% opacity
text_color: Color(1, 1, 0.9, 1)               # Light yellow

# Pressed
background: Color(0.006, 0.33, 0.5, 0.35)     # Darker blue, 35% opacity
text_color: Color(0.9, 0.9, 0.9, 1)           # Light grey

# Disabled
background: Color(0.3, 0.3, 0.3, 0.15)        # Grey (same as standard)
text_color: Color(0.5, 0.5, 0.5, 0.6)         # Dimmed grey
```

**Examples**: Yes, OK, Confirm, Accept, Save, Submit

### 3. ForwardNavButton (Light Blue)

**Usage**: Navigation forward into scenes/areas

**States**:
```gdscript
# Normal
background: Color(0.255, 0.412, 0.882, 0.2)   # Royal blue #4169E1, 20% opacity
border: 2px, Color(0.357, 0.608, 0.835, 1.0)  # Bright blue #5B9BD5 (THICK BORDER)
corner_radius: 6px (more rounded than standard)
shadow: Color(0, 0, 0, 0.1), size 4px, offset (0, 2)
text_color: Color(0.529, 0.808, 0.922, 1.0)   # Sky blue #87CEEB

# Hover
background: Color(0.3, 0.5, 0.95, 0.2)        # Lighter blue, 20% opacity
border: 2px, Color(0.4, 0.65, 0.88, 1.0)      # Brighter border
shadow: size 6px, offset (0, 3)               # Grows on hover
text_color: Color(0.65, 0.88, 0.98, 1.0)      # Brighter sky blue

# Pressed
background: Color(0.2, 0.35, 0.75, 0.25)      # Darker blue, 25% opacity
border: 2px, Color(0.3, 0.55, 0.78, 1.0)
shadow: size 2px, offset (0, 1)               # Shrinks on press
text_color: Color(0.45, 0.73, 0.86, 1.0)      # Dimmed blue

# Disabled
background: Color(0.3, 0.3, 0.3, 0.15)        # Grey
border: 1px, Color(0.4, 0.4, 0.4, 0.3)        # Subtle grey border
text_color: Color(0.5, 0.5, 0.5, 0.6)         # Dimmed grey
```

**Visual Features**:
- **2px border** (thicker = more prominent)
- **6px corner radius** (rounder = friendlier)
- **Shadow** (depth effect, animates on hover/press)
- Light blue color temperature

**Examples**: Enter, Explore, Continue, Next, Open, Advance, Go

### 4. BackNavButton (Navy Blue)

**Usage**: Navigation back to previous scenes

**States**:
```gdscript
# Normal
background: Color(0, 0, 0.502, 0.3)           # Navy #000080, 30% opacity
border: 1px, Color(0.176, 0.290, 0.431, 1.0)  # Blue-gray #2D4A6E (THIN BORDER)
corner_radius: 6px (matches forward nav)
shadow: Color(0, 0, 0, 0.08), size 2px, offset (0, 1)
text_color: Color(0.690, 0.769, 0.871, 1.0)   # Steel blue #B0C4DE

# Hover
background: Color(0.05, 0.05, 0.6, 0.35)      # Slightly lighter navy, 35% opacity
border: 1px, Color(0.22, 0.35, 0.5, 1.0)      # Lighter border
shadow: size 3px, offset (0, 2)               # Slight grow
text_color: Color(0.78, 0.85, 0.94, 1.0)      # Brighter steel blue

# Pressed
background: Color(0, 0, 0.4, 0.4)             # Darker navy, 40% opacity
border: 1px, Color(0.15, 0.25, 0.38, 1.0)     # Darker border
shadow: size 1px                              # Minimal shadow
text_color: Color(0.60, 0.69, 0.80, 1.0)      # Dimmed steel blue

# Disabled
background: Color(0.3, 0.3, 0.3, 0.15)        # Grey
border: 1px, Color(0.4, 0.4, 0.4, 0.3)        # Subtle grey border
text_color: Color(0.5, 0.5, 0.5, 0.6)         # Dimmed grey
```

**Visual Features**:
- **1px border** (thinner = less prominent than forward)
- **6px corner radius** (matches forward nav)
- **Minimal shadow** (more subtle than forward)
- Dark blue color temperature

**Examples**: Return, Exit, Leave, Previous, Back, Close

### 5. DangerButton (Red)

**Usage**: Destructive or irreversible actions that require extra caution

**States**:
```gdscript
# Normal
background: Color(0.8, 0.1, 0.1, 0.25)            # Dark red #CC1A1A, 25% opacity
border: 2px, Color(1.0, 0.3, 0.3, 0.6)            # Bright red #FF4D4D (WARNING BORDER)
corner_radius: 3px
text_color: Color(1, 0.9, 0.9, 1)                 # Light pink-white

# Hover
background: Color(0.9, 0.2, 0.2, 0.3)             # Brighter red, 30% opacity
border: 2px, Color(1.0, 0.4, 0.4, 0.7)            # Even brighter border
text_color: Color(1, 1, 0.9, 1)                   # Light yellow (warning!)

# Pressed
background: Color(0.6, 0.05, 0.05, 0.35)          # Darker red, 35% opacity
border: 2px, Color(0.8, 0.2, 0.2, 0.6)            # Darker border
text_color: Color(0.95, 0.85, 0.85, 1)            # Dimmed pink-white

# Disabled
background: Color(0.3, 0.3, 0.3, 0.15)            # Grey (same as other buttons)
border: 1px, Color(0.4, 0.4, 0.4, 0.3)            # Subtle grey border
text_color: Color(0.5, 0.5, 0.5, 0.6)             # Dimmed grey
```

**Visual Features**:
- **2px border** (thick warning border, like forward nav)
- **Red color temperature** (danger, caution, stop)
- **High contrast** (stands out from all other buttons)
- **Light pink-white text** (softer than pure white on red)

**Examples**: Delete Save, Permanently Remove, Destroy Item, Quit Without Saving, Reset Progress

**Usage Guidelines**:
- Use sparingly - only for truly destructive actions
- Consider adding confirmation dialogs for danger buttons
- Never use for navigation or neutral actions

---

## Button State Reference Table

| State | Background Opacity | Border | Text Color | Shadow | Clickable |
|-------|-------------------|--------|------------|--------|-----------|
| Normal | 20-30% | 1-2px | White | 2-4px | Yes |
| Hover | 20-35% | 1-2px (brighter) | Light yellow | 3-6px | Yes |
| Pressed | 25-40% | 1-2px (darker) | Light grey | 1-2px | Yes |
| Disabled | 15% | None/subtle | Dimmed grey | None | No |

**Key Insight**: Disabled state has **obviously lower opacity** and **grey colors** to make it clear it's not clickable.

---

## Other UI Elements

### Panel

```gdscript
background: Color(0.58, 0.247, 0.012, 0.3)    # Dark orange, 30% opacity
corner_radius: 4px
border: None (optional 1px subtle border)
```

**Usage**: Container panels in menu, info displays

### Label

```gdscript
background: None (transparent)                # No background - text only
# OR for highlighted labels:
background: Color(0.2, 0.2, 0.2, 0.5)         # Dark grey, 50% opacity (distinct from panel)
corner_radius: 3px
text_color: Color(1, 1, 1, 1)                 # White
line_spacing: 2px
```

**Usage**: Stat displays, counters, resource amounts

**Note**: Labels should generally have NO background to avoid color collision when placed on panels. Use dark grey background only for special highlighted labels that need emphasis.

### ProgressBar

**Template Structure**:

All progress bars follow this uniform template:

```gdscript
# Background (empty part of bar)
background: Color(0.2, 0.2, 0.2, 0)           # Transparent dark grey
border: 1px, Color(0.3, 0.3, 0.3, 0.5)        # Optional subtle border
corner_radius: 2px
height: 20px (scaled with resolution)

# Fill (filled part of bar)
fill_color: [SEE VARS BELOW]                  # Color varies by bar type
corner_radius: 2px
opacity: 30%
```

**How to Use**:
1. Create ProgressBar node in scene
2. Apply default theme (handles background/structure)
3. Reference a named bar from vars below
4. Set fill color via theme override or StyleBoxFlat

---

### ProgressBar Variables

Define specific progress bars here with their purpose and colors:

```gdscript
# Named Progress Bars
progress_bars = {
    # Positive Resource Bars (Green)
    "health": {
        "label": "Health",
        "fill_color": Color(0, 1, 0, 0.3),          # Green, 30% opacity
        "usage": "Player health, HP"
    },
    "stamina": {
        "label": "Stamina",
        "fill_color": Color(0.2, 0.9, 0.3, 0.3),    # Yellow-green, 30% opacity
        "usage": "Energy, endurance"
    },

    # Danger/Warning Bars (Orange/Red)
    "suspicion": {
        "label": "Suspicion",
        "fill_color": Color(0.9, 0.4, 0.05, 0.3),   # Orange, 30% opacity
        "usage": "Guard suspicion meter"
    },
    "heat": {
        "label": "Heat",
        "fill_color": Color(0.95, 0.5, 0.1, 0.3),   # Bright orange, 30% opacity
        "usage": "Furnace heat level"
    },

    # Progress/Experience Bars (Blue)
    "xp": {
        "label": "Experience",
        "fill_color": Color(0.3, 0.6, 1, 0.3),      # Light blue, 30% opacity
        "usage": "XP progress to next level"
    },
    "focus": {
        "label": "Focus",
        "fill_color": Color(0.3, 0.6, 1, 0.3),      # Light blue, 30% opacity
        "usage": "Mental energy, concentration"
    },

    # Add more bars as needed for game systems
}
```

**Adding New Bars**:
1. Add entry to `progress_bars` dict above
2. Specify label, fill_color, and usage
3. Document in implementation notes which scenes use it

---

## Typography

**All font sizing is defined in Phase 1.10 (Responsive Layout).** This plan references those values.

### Font Size Reference

See **Phase 1.10 (Responsive Layout) - Typography & Font Sizing section** for:
- `BASE_FONT_SIZE` (25px at 1280x720)
- Font size constants (FONT_NORMAL, FONT_SMALL, FONT_LARGE, FONT_HUGE)
- Scaling behavior (25px → 37px at 1080p, capped at 1.5x)
- Usage guidelines and hierarchy

### Button Sizing Reference

See **Phase 1.10 (Responsive Layout) - Button Sizing Guidelines section** for:
- `MIN_BUTTON_WIDTH` (120px)
- `MIN_BUTTON_HEIGHT` (50px)
- Button padding constants
- Scaling rules

### Theme Implementation

The theme does NOT specify font sizes directly. Instead:
- Font sizes are applied automatically by `ResponsiveLayout.apply_to_scene()`
- All Labels, Buttons, and RichTextLabels get `FONT_NORMAL` (25px scaled) by default
- Manual overrides only for special cases (titles, debug text, etc.)

**Key Insight**: ResponsiveLayout handles ALL font scaling. The theme only handles colors, borders, padding, and visual styling.

---

## Button Animation & Interactivity

**NOTE**: This section will be fleshed out in a future implementation phase.

### Animation Requirements

Button state transitions (Normal → Hover → Pressed) currently have no animation specified. Future implementation should address:

1. **Transition Duration**: How long does color/scale change take? (Suggested: 0.1-0.2s)
2. **Easing Function**: Linear? Ease-out? (Suggested: ease-out for natural feel)
3. **Properties to Animate**:
   - Background color fade
   - Text color fade
   - Border color fade
   - Shadow size/offset (for ForwardNav buttons)
   - Scale (optional micro-scale on press: 0.98x)

4. **Implementation Method**:
   - Tween-based animation in code?
   - AnimationPlayer nodes?
   - Shader-based transitions?
   - Theme doesn't natively animate - requires scripting

### Shadow Performance

**NOTE**: Shadow performance optimization will be addressed in the animation section.

ForwardNav buttons use StyleBoxFlat shadows that animate on hover/press. Considerations:

1. **Performance Cost**: Multiple shadows can impact frame rate on low-end systems
2. **Testing Needed**: Profile with 20+ buttons on screen simultaneously
3. **Fallback Options**:
   - Make shadows optional in settings
   - Disable shadows below certain FPS threshold
   - Use alternative visual effect (border glow) instead

4. **Implementation Notes**:
   - Document shadow performance in testing phase
   - Consider togglable "reduced motion" accessibility option

---

## Implementation Tasks

### 1. Create Theme Resource

**Steps**:
1. In Godot Editor: FileSystem → Right-click root
2. Create → Resource → Theme
3. Save as: `res://default_theme.tres`

### 2. Define Base Button Styles

**In default_theme.tres** (via Inspector or text editor):

Create StyleBoxFlat resources for each button type and state:
- StyleBoxFlat_button_normal (orange)
- StyleBoxFlat_button_hover (orange)
- StyleBoxFlat_button_pressed (orange)
- StyleBoxFlat_button_disabled (grey)

Apply to Button type in theme editor.

### 3. Create Theme Variations

**For specialized buttons**, create type variations:

```gdscript
# In default_theme.tres
[resource]
AffirmativeButton/base_type = &"Button"
AffirmativeButton/styles/normal = SubResource("StyleBoxFlat_affirmative_button_normal")
AffirmativeButton/styles/hover = SubResource("StyleBoxFlat_affirmative_button_hover")
AffirmativeButton/styles/pressed = SubResource("StyleBoxFlat_affirmative_button_pressed")
AffirmativeButton/styles/disabled = SubResource("StyleBoxFlat_button_disabled")

ForwardNavButton/base_type = &"Button"
ForwardNavButton/styles/normal = SubResource("StyleBoxFlat_forward_nav_button_normal")
# ... etc

BackNavButton/base_type = &"Button"
BackNavButton/styles/normal = SubResource("StyleBoxFlat_back_nav_button_normal")
# ... etc

DangerButton/base_type = &"Button"
DangerButton/styles/normal = SubResource("StyleBoxFlat_danger_button_normal")
DangerButton/styles/hover = SubResource("StyleBoxFlat_danger_button_hover")
DangerButton/styles/pressed = SubResource("StyleBoxFlat_danger_button_pressed")
DangerButton/styles/disabled = SubResource("StyleBoxFlat_button_disabled")
```

### 4. Apply in Scenes

**Standard Button** (default):
```gdscript
# No extra code needed - inherits from theme automatically
var button = Button.new()
```

**Affirmative Button**:
```gdscript
# In scene or code:
button.theme_type_variation = &"AffirmativeButton"
```

**Forward Nav Button**:
```gdscript
button.theme_type_variation = &"ForwardNavButton"
```

**Back Nav Button**:
```gdscript
button.theme_type_variation = &"BackNavButton"
```

**Danger Button**:
```gdscript
button.theme_type_variation = &"DangerButton"
```

### 5. Set as Project Default

**File**: `project.godot`

```ini
[gui]

theme/custom="res://default_theme.tres"
```

---

## Testing Strategy

### Manual Test Criteria

**Theme Creation:**
- [ ] default_theme.tres created successfully
- [ ] Set as default theme in project settings
- [ ] All scenes automatically use theme

**Button Types:**
- [ ] Standard button: Orange, white text
- [ ] Affirmative button: Blue, white text
- [ ] ForwardNav button: Light blue, 2px border, shadow
- [ ] BackNav button: Navy blue, 1px border, minimal shadow
- [ ] Danger button: Red, pink-white text, 2px warning border

**Button States:**
- [ ] Normal: Correct colors for each type
- [ ] Hover: Lighter background, yellow text
- [ ] Pressed: Darker background, grey text
- [ ] Disabled: Grey background, dimmed text, obviously unclickable

**Visual Hierarchy:**
- [ ] Forward nav more prominent than back nav (thicker border)
- [ ] Affirmative stands out from standard (blue vs orange)
- [ ] Danger button clearly stands out (red, warning border)
- [ ] Disabled clearly looks inactive

**Cross-Scene Consistency:**
- [ ] Create test scene with all button types
- [ ] All inherit theme automatically
- [ ] No per-scene styling needed

---

## Files to Create

- `res://default_theme.tres` - Main theme resource

## Files to Modify

- `project.godot` - Set default theme in GUI settings

---

## Design Rationale

### Why This Color Scheme?

**Orange base**:
- Warm, industrial, matches furnace/coal theme
- Attention-grabbing without being alarming
- Good contrast with blue accents

**Blue accents**:
- Cool, trustworthy, clear distinction from orange
- Light blue = forward (optimistic, progress)
- Navy blue = back (retreat, safety)

**30% opacity**:
- Translucent overlays maintain visual layering
- Background visible through UI (immersive)
- High enough for readability, low enough for depth

### Border Strategy

**Navigation buttons get prominent borders**:
- Forward: 2px (primary action)
- Back: 1px (secondary action)
- Creates instant visual hierarchy

**Standard/affirmative: subtle or no border**:
- Focus is on color, not outline
- Cleaner appearance

### Shadow Usage

**Forward nav only**:
- Adds depth, makes button "pop"
- Reinforces it's the primary action
- Animates on hover/press (tactile feedback)

**Others: no shadow**:
- Keeps design clean
- Prevents visual clutter

---

## Notes & Decisions

**Decision 1**: Orange/blue scheme (not silver/gold/red/green)
- **Rationale**: Matches current implementation, industrial aesthetic
- **Benefit**: Clear visual language (orange = standard, blue = affirmative/navigation)

**Decision 2**: Four button types instead of two
- **Rationale**: Navigation needs visual distinction from dialog actions
- **Benefit**: Players instantly know forward vs back, confirm vs cancel

**Decision 3**: Border thickness communicates hierarchy
- **Rationale**: Subtle visual cue that doesn't require color perception
- **Accessibility**: Works for colorblind players

**Decision 4**: Obviously disabled state (grey, low opacity)
- **Rationale**: Disabled buttons must be unmistakably inactive
- **Implementation**: 15% opacity grey, dimmed text, no hover effect

**Decision 5**: Unified 25px base font
- **Rationale**: Simplicity, consistency, scales with resolution
- **Benefit**: One size to maintain, easy to adjust globally

**Decision 6**: 30% panel opacity
- **Rationale**: Sweet spot between readability and translucency
- **Tested**: Lower = too transparent, higher = loses layered aesthetic

---

## Implementation Checklist

Before marking this phase complete:

- [ ] default_theme.tres created
- [ ] Font sizing references ResponsiveLayout.BASE_FONT_SIZE (25px)
- [ ] Button sizing follows ResponsiveLayout guidelines
- [ ] Standard button styles (normal/hover/pressed/disabled)
- [ ] Affirmative button variation
- [ ] ForwardNav button variation
- [ ] BackNav button variation
- [ ] Danger button variation
- [ ] Panel styles
- [ ] Label styles (transparent background by default)
- [ ] ProgressBar template structure defined
- [ ] Named progress bars: health, stamina, suspicion, heat, xp
- [ ] Theme set as project default
- [ ] Test scene created with all button types
- [ ] All button states tested (normal/hover/pressed/disabled)
- [ ] Visual hierarchy verified (forward > back, danger stands out, affirmative vs standard)
- [ ] Cross-scene consistency confirmed
- [ ] Disabled state obviously unclickable
- [ ] Animation section notes added for future implementation
- [ ] Shadow performance notes added for future optimization

---

**Last Updated**: 2025-12-05
**Maintainer**: Claude + User collaboration
